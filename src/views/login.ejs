<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In</title>

    <!-- CSS Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css" rel="stylesheet">
</head>

<style>
    .logo {
        display: block;
        margin: 0 auto 20px auto; /* Gambar ditengah dengan jarak bawah 20px */
        width: 100px; /* Ukuran gambar disesuaikan */
        height: auto; /* Menjaga proporsi gambar */
        background: none; /* Pastikan tidak ada background */
    }

    body, html {
        font-family: Quicksand, sans-serif !important;
        height: 100%;
        padding-top: 2rem;
        background-color: #4b4a4a;
    }
    .card {
        padding: 2rem;
        margin: 2rem;
        border-radius: 5px; /* Mengurangi kelengkungan sudut */
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0);
    }
    .btn {
        padding: 0.4rem 3rem;
        display: block;
        margin: 0 auto; /* Tombol ditengah */
		width: auto;
    }
    .form-footer {
        margin-top: 10px;
        text-align: center; /* Teks dan link ditengah */
    }
    .form-footer a i {
        font-style: italic;
		text-decoration: underline;
    }
    .logo {
        display: block;
        margin: 0 auto 20px auto; /* Gambar ditengah dengan jarak bawah 20px */
        width: 100px; /* Ukuran gambar disesuaikan */
        height: auto; /* Menjaga proporsi gambar */
    }
</style>

<body style="padding: 0px !important;">
    <div class="container h-100">
        <div class="row h-100 justify-content-center align-items-center">
            <form action="<%= url %>login/auth" method="POST" class="col-md-4 card bg-light">
                
			    <!-- Logo Gambar -->
                <img src="/public/images/buku.png" alt="Logo" class="logo">

                <h2 >Sign In</h2>
                <h6 >Masukkan username dan password Anda</h6>
                
                <!-- Input Username -->
                <div class="mt-4">
					<input type="text" name="username" class="form-control" placeholder="Username" required>
                </div>
                
                <!-- Input Password -->
                <div class="mt-4">
                    <input type="password" name="pass" class="form-control" placeholder="Password" required>
                </div>
                
                <!-- Link "Belum memiliki akun?" -->
                <div class="form-footer mt-4">
                    <span>Belum memiliki akun? <a href="<%= url %>register"><i>Daftar di sini</i></a></span>
                </div>
                
				<!-- Tombol Submit -->
				<button type="submit" class="btn btn-primary mt-3">Sign In</button>

                <!-- Pesan Flash -->
                <% if (pesanFlash) { %>
                    <div class="alert alert-<%= colorFlash %> alert-dismissible fade show mt-3" role="alert">
                        <strong><%= statusFlash %></strong> <%= pesanFlash %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
            </form>
        </div>  
    </div>

    <!-- JS Bootstrap 5 -->
    <!-- Bootstrap 5.3.0 tanpa atribut integrity -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
